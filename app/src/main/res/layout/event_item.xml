<?xml version="1.0" encoding="utf-8"?>

<!-- Layoutet for ét element i listen (én begivenhed) -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="12dp">

    <!-- Viser navnet på begivenheden -->
    <TextView
        android:id="@+id/eventNameTextView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="20sp"
        android:textStyle="bold" />

    <!-- Viser datoen for begivenheden -->
    <TextView
        android:id="@+id/eventDateTextView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="16sp" />

    <!-- Viser en kort beskrivelse af begivenheden -->
    <TextView
        android:id="@+id/eventShortDescriptionTextView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="15sp"
        android:paddingBottom="8dp" />

    <!-- Knap der åbner detaljeskærmen -->
    <Button
        android:id="@+id/detailsButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Detaljer" />

    <!-- Knap der viser en dialogboks for tilmelding -->
    <Button
        android:id="@+id/signupButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Tilmeld" />

</LinearLayout>