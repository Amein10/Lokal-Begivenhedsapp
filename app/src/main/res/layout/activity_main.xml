<?xml version="1.0" encoding="utf-8"?>

<!-- Layoutet for startskærmen, hvor alle begivenheder vises -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- Logo for foreningen, hentet fra drawable-mappen -->
    <ImageView
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:src="@drawable/logo"
        android:layout_gravity="center"
        android:layout_marginBottom="16dp" />

    <!-- Overskrift for siden -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Kommende begivenheder"
        android:textSize="24sp"
        android:textStyle="bold"
        android:paddingBottom="12dp" />

    <!-- Inputfelt hvor brugeren kan skrive en søgning -->
    <EditText
        android:id="@+id/searchEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Søg efter begivenhed" />

    <!-- Knap der starter søgningen og filtrerer listen -->
    <Button
        android:id="@+id/searchButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Søg" />

    <!-- Liste der viser alle begivenheder via EventAdapter -->
    <ListView
        android:id="@+id/eventListView"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

</LinearLayout>